version: 2
jobs:
  build:
    docker:
      - image: circleci/node:12.18.3
    steps:
      - checkout
      - run:
          name: npm install
          command: npm install
      - run:
          name: build
          command: npm run build
